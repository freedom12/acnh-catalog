# 项目架构说明

## 📁 目录结构

```
src/
├── components/          # UI组件
│   ├── TabSelector.vue      # 标签页选择器
│   ├── FilterControls.vue   # 筛选控件
│   ├── ItemsGrid.vue        # 物品网格
│   ├── VillagersGrid.vue    # 村民网格
│   ├── NPCsGrid.vue         # NPC网格
│   ├── CreaturesGrid.vue    # 生物网格
│   ├── ReactionsGrid.vue    # 表情网格
│   ├── ItemCard.vue         # 物品卡片
│   ├── Pagination.vue       # 分页组件
│   └── StatsDisplay.vue     # 统计显示
│
├── composables/        # 组合函数（业务逻辑）
│   ├── useItemsData.ts      # 物品数据管理
│   ├── useVillagersData.ts  # 村民数据管理
│   ├── useNPCsData.ts       # NPC数据管理
│   ├── useCreaturesData.ts  # 生物数据管理
│   ├── useReactionsData.ts  # 表情数据管理
│   ├── useItemsFilter.ts    # 物品筛选逻辑
│   ├── useFilterOptions.ts  # 筛选选项管理
│   ├── useColorDisplay.ts   # 颜色显示
│   ├── useDebounce.ts       # 防抖工具
│   └── index.ts             # 统一导出
│
├── config/             # 配置文件
│   └── index.ts             # 应用配置（数据路径、分页等）
│
├── constants/          # 常量定义
│   └── index.ts             # 统一常量（文本、图标、映射等）
│
├── models/             # 数据模型
│   ├── ItemModel.ts         # 物品模型类
│   └── index.ts             # 统一导出
│
├── router/             # 路由配置
│   └── index.ts             # Vue Router配置
│
├── services/           # 服务层（数据加载、API调用）
│   ├── dataService.ts       # 数据加载服务
│   └── filterService.ts     # 筛选服务
│
├── types/              # TypeScript类型定义
│   ├── index.ts             # 通用类型和统一导出
│   ├── item.ts              # 物品相关类型
│   ├── villager.ts          # 村民类型
│   ├── npc.ts               # NPC类型
│   ├── creature.ts          # 生物类型
│   └── reaction.ts          # 表情类型
│
├── utils/              # 工具函数
│   ├── itemHelpers.ts       # 物品相关工具函数
│   ├── common.ts            # 通用工具函数
│   └── index.ts             # 统一导出
│
├── views/              # 页面组件
│   ├── HomePage.vue         # 首页（标签页容器）
│   ├── ItemsTab.vue         # 物品标签页
│   ├── VillagersTab.vue     # 村民标签页
│   ├── NPCsTab.vue          # NPC标签页
│   ├── CreaturesTab.vue     # 生物标签页
│   ├── ReactionsTab.vue     # 表情标签页
│   └── ItemDetailPage.vue   # 物品详情页
│
├── App.vue             # 根组件
├── main.ts             # 应用入口
└── style.css           # 全局样式
```

## 🏗️ 架构设计原则

### 1. 关注点分离
- **组件 (Components)**: 只负责UI渲染，不包含业务逻辑
- **组合函数 (Composables)**: 封装可复用的业务逻辑和状态管理
- **服务 (Services)**: 处理数据加载和外部API调用
- **工具 (Utils)**: 提供纯函数工具，无副作用

### 2. 模块化
每个功能模块独立管理：
- Items (物品)
- Villagers (村民)
- NPCs (NPC)
- Creatures (生物)
- Reactions (表情)

### 3. 类型安全
- 所有数据结构都有完整的TypeScript类型定义
- 类型定义按模块分类，便于维护
- 使用统一的`Translation`接口避免重复

### 4. 常量管理
- 所有硬编码的文本、图标、映射关系统一在`constants`中管理
- 便于国际化和主题定制
- 避免魔法数字和字符串

### 5. 代码复用
- 通用工具函数提取到`utils/common.ts`
- 业务逻辑封装在composables中
- 组件保持简洁，专注于渲染

## 📝 命名规范

### 文件命名
- 组件: PascalCase (如 `ItemCard.vue`)
- 工具函数: camelCase (如 `itemHelpers.ts`)
- 类型文件: camelCase (如 `item.ts`)
- 常量文件: camelCase (如 `index.ts`)

### 变量命名
- 普通变量: camelCase (如 `allItems`)
- 常量: UPPER_SNAKE_CASE (如 `DATA_LOADING`)
- 组件: PascalCase (如 `ItemCard`)
- 类型/接口: PascalCase (如 `Item`, `Villager`)

### 函数命名
- 普通函数: camelCase (如 `formatPrice`)
- Composables: use前缀 + camelCase (如 `useItemsData`)
- 事件处理: handle前缀 + camelCase (如 `handleClick`)

## 🔄 数据流

```
用户交互
    ↓
组件 (Components)
    ↓
组合函数 (Composables)
    ↓
服务 (Services)
    ↓
数据源 (JSON Files)
```

## 🎯 重构改进点

1. **统一常量管理**: 创建`constants/index.ts`集中管理所有常量
2. **提取通用工具**: 创建`utils/common.ts`提供通用辅助函数
3. **优化类型定义**: 将item相关类型提取到`types/item.ts`
4. **消除重复代码**: 使用统一的`Translation`类型
5. **改进可维护性**: 所有Grid组件使用统一的工具函数和常量
6. **增强可读性**: 添加完整的JSDoc注释
